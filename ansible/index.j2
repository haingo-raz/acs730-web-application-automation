<!DOCTYPE html>
<html>
<head>
    <title>ACS730 NH Group</title>
    <style>
        body {
            font-family: 'Comic Sans MS', cursive, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #ffe6f2;
            color: #880e4f;
        }

        .pink-box {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: #ffcce6;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            border-radius: 12px;
            border: 2px solid #ff80bf;
        }

        h1 {
            color: #ad1457;
            text-align: center;
            font-size: 28px;
        }

        p {
            font-size: 20px;
            text-align: center;
            color: #d81b60;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        ul li {
            font-size: 18px;
            margin: 10px 0;
            padding: 12px;
            background: #ffe6f2;
            border-radius: 6px;
            text-align: center;
            border: 1px solid #ff99cc;
        }

        img {
            display: block;
            max-width: 100%;
            height: auto;
            margin: 20px auto;
            border-radius: 12px;
            border: 2px solid #ff80bf;
        }
    </style>
</head>
<body>
    <div class="pink-box">
        <h1>Showing content from webserver with {{ instance_id }} ID</h1>
        <p>Group: {{ team_name }}</p>
        <ul>
            <li>{{ member1_name }}</li>
            <li>{{ member2_name }}</li>
        </ul>
        <p>Using Terraform with Ansible</p>
        <img src="{{ image_url }}" alt="Terraform and Ansible">
    </div>
</body>
</html>